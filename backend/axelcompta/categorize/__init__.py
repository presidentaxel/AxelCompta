"""categorize — pipeline hybride règles → ML → LLM → revue humaine (doc 05).

Produit des ProposedEntry ; seul `workflow` peut les transformer en écritures
via `ledger`, après validation (doc 03 §3). Dépend de core, packs, documents.
"""

from __future__ import annotations
